// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/tokenmngr/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Mintperm } from "./mintperm";
import { Options } from "./options";
import { Params } from "./params";
import { Token } from "./token";
import { TokenBurn } from "./token_burn";

export const protobufPackage = "sixprotocol.tokenmngr";

/** GenesisState defines the tokenmngr module's genesis state. */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  tokenList: Token[];
  mintpermList: Mintperm[];
  tokenBurnList: TokenBurn[];
  options: Options | undefined;
}

function createBaseGenesisState(): GenesisState {
  return { params: undefined, tokenList: [], mintpermList: [], tokenBurnList: [], options: undefined };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.tokenList) {
      Token.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.mintpermList) {
      Mintperm.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.tokenBurnList) {
      TokenBurn.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.options !== undefined) {
      Options.encode(message.options, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tokenList.push(Token.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mintpermList.push(Mintperm.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tokenBurnList.push(TokenBurn.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.options = Options.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      tokenList: globalThis.Array.isArray(object?.tokenList) ? object.tokenList.map((e: any) => Token.fromJSON(e)) : [],
      mintpermList: globalThis.Array.isArray(object?.mintpermList)
        ? object.mintpermList.map((e: any) => Mintperm.fromJSON(e))
        : [],
      tokenBurnList: globalThis.Array.isArray(object?.tokenBurnList)
        ? object.tokenBurnList.map((e: any) => TokenBurn.fromJSON(e))
        : [],
      options: isSet(object.options) ? Options.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.tokenList?.length) {
      obj.tokenList = message.tokenList.map((e) => Token.toJSON(e));
    }
    if (message.mintpermList?.length) {
      obj.mintpermList = message.mintpermList.map((e) => Mintperm.toJSON(e));
    }
    if (message.tokenBurnList?.length) {
      obj.tokenBurnList = message.tokenBurnList.map((e) => TokenBurn.toJSON(e));
    }
    if (message.options !== undefined) {
      obj.options = Options.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.tokenList = object.tokenList?.map((e) => Token.fromPartial(e)) || [];
    message.mintpermList = object.mintpermList?.map((e) => Mintperm.fromPartial(e)) || [];
    message.tokenBurnList = object.tokenBurnList?.map((e) => TokenBurn.fromPartial(e)) || [];
    message.options = (object.options !== undefined && object.options !== null)
      ? Options.fromPartial(object.options)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
