// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/nftmngr/metadata_creator.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sixprotocol.nftmngr";

export interface MapTokenToMinter {
  tokenId: string;
  minter: string;
}

export interface MetadataCreator {
  nftSchemaCode: string;
  metadataMintedBy: MapTokenToMinter[];
}

function createBaseMapTokenToMinter(): MapTokenToMinter {
  return { tokenId: "", minter: "" };
}

export const MapTokenToMinter: MessageFns<MapTokenToMinter> = {
  encode(message: MapTokenToMinter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenId !== "") {
      writer.uint32(10).string(message.tokenId);
    }
    if (message.minter !== "") {
      writer.uint32(18).string(message.minter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MapTokenToMinter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMapTokenToMinter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tokenId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.minter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MapTokenToMinter {
    return {
      tokenId: isSet(object.tokenId) ? globalThis.String(object.tokenId) : "",
      minter: isSet(object.minter) ? globalThis.String(object.minter) : "",
    };
  },

  toJSON(message: MapTokenToMinter): unknown {
    const obj: any = {};
    if (message.tokenId !== "") {
      obj.tokenId = message.tokenId;
    }
    if (message.minter !== "") {
      obj.minter = message.minter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MapTokenToMinter>, I>>(base?: I): MapTokenToMinter {
    return MapTokenToMinter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MapTokenToMinter>, I>>(object: I): MapTokenToMinter {
    const message = createBaseMapTokenToMinter();
    message.tokenId = object.tokenId ?? "";
    message.minter = object.minter ?? "";
    return message;
  },
};

function createBaseMetadataCreator(): MetadataCreator {
  return { nftSchemaCode: "", metadataMintedBy: [] };
}

export const MetadataCreator: MessageFns<MetadataCreator> = {
  encode(message: MetadataCreator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nftSchemaCode !== "") {
      writer.uint32(10).string(message.nftSchemaCode);
    }
    for (const v of message.metadataMintedBy) {
      MapTokenToMinter.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetadataCreator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadataCreator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nftSchemaCode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.metadataMintedBy.push(MapTokenToMinter.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetadataCreator {
    return {
      nftSchemaCode: isSet(object.nftSchemaCode) ? globalThis.String(object.nftSchemaCode) : "",
      metadataMintedBy: globalThis.Array.isArray(object?.metadataMintedBy)
        ? object.metadataMintedBy.map((e: any) => MapTokenToMinter.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MetadataCreator): unknown {
    const obj: any = {};
    if (message.nftSchemaCode !== "") {
      obj.nftSchemaCode = message.nftSchemaCode;
    }
    if (message.metadataMintedBy?.length) {
      obj.metadataMintedBy = message.metadataMintedBy.map((e) => MapTokenToMinter.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetadataCreator>, I>>(base?: I): MetadataCreator {
    return MetadataCreator.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetadataCreator>, I>>(object: I): MetadataCreator {
    const message = createBaseMetadataCreator();
    message.nftSchemaCode = object.nftSchemaCode ?? "";
    message.metadataMintedBy = object.metadataMintedBy?.map((e) => MapTokenToMinter.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
