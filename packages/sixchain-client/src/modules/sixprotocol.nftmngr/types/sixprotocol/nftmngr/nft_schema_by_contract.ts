// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/nftmngr/nft_schema_by_contract.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sixprotocol.nftmngr";

export interface NFTSchemaByContract {
  originContractAddress: string;
  schemaCodes: string[];
}

function createBaseNFTSchemaByContract(): NFTSchemaByContract {
  return { originContractAddress: "", schemaCodes: [] };
}

export const NFTSchemaByContract: MessageFns<NFTSchemaByContract> = {
  encode(message: NFTSchemaByContract, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.originContractAddress !== "") {
      writer.uint32(10).string(message.originContractAddress);
    }
    for (const v of message.schemaCodes) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NFTSchemaByContract {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNFTSchemaByContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.originContractAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.schemaCodes.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NFTSchemaByContract {
    return {
      originContractAddress: isSet(object.originContractAddress) ? globalThis.String(object.originContractAddress) : "",
      schemaCodes: globalThis.Array.isArray(object?.schemaCodes)
        ? object.schemaCodes.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: NFTSchemaByContract): unknown {
    const obj: any = {};
    if (message.originContractAddress !== "") {
      obj.originContractAddress = message.originContractAddress;
    }
    if (message.schemaCodes?.length) {
      obj.schemaCodes = message.schemaCodes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NFTSchemaByContract>, I>>(base?: I): NFTSchemaByContract {
    return NFTSchemaByContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NFTSchemaByContract>, I>>(object: I): NFTSchemaByContract {
    const message = createBaseNFTSchemaByContract();
    message.originContractAddress = object.originContractAddress ?? "";
    message.schemaCodes = object.schemaCodes?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
