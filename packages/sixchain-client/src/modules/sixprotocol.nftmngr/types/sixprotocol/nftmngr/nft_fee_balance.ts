// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/nftmngr/nft_fee_balance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sixprotocol.nftmngr";

export interface NFTFeeBalance {
  /** map<int32, string> fee_balances = 1; */
  feeBalances: string[];
}

function createBaseNFTFeeBalance(): NFTFeeBalance {
  return { feeBalances: [] };
}

export const NFTFeeBalance: MessageFns<NFTFeeBalance> = {
  encode(message: NFTFeeBalance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.feeBalances) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NFTFeeBalance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNFTFeeBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.feeBalances.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NFTFeeBalance {
    return {
      feeBalances: globalThis.Array.isArray(object?.feeBalances)
        ? object.feeBalances.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: NFTFeeBalance): unknown {
    const obj: any = {};
    if (message.feeBalances?.length) {
      obj.feeBalances = message.feeBalances;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NFTFeeBalance>, I>>(base?: I): NFTFeeBalance {
    return NFTFeeBalance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NFTFeeBalance>, I>>(object: I): NFTFeeBalance {
    const message = createBaseNFTFeeBalance();
    message.feeBalances = object.feeBalances?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
