// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/nftoracle/sync_action_signer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { RequestStatus, requestStatusFromJSON, requestStatusToJSON } from "./request";

export const protobufPackage = "sixprotocol.nftoracle";

export interface SyncActionSigner {
  id: number;
  chain: string;
  actorAddress: string;
  ownerAddress: string;
  /** will use as creator when message is success */
  caller: string;
  requiredConfirm: number;
  status: RequestStatus;
  currentConfirm: number;
  confirmers: string[];
  createdAt: Date | undefined;
  validUntil: Date | undefined;
  dataHashes: ContractInfoHash[];
  expiredHeight: number;
  executionErrorMessage: string;
}

export interface ParameterSyncSignerByOracle {
  chain: string;
  /** get from smart contract */
  ownerAddress: string;
  /** get from smart contract */
  actorAddress: string;
  /** get from smart contract */
  expireEpoch: string;
}

export interface ContractInfoHash {
  contractParam: ParameterSyncSignerByOracle | undefined;
  hash: Uint8Array;
  confirmers: string[];
}

function createBaseSyncActionSigner(): SyncActionSigner {
  return {
    id: 0,
    chain: "",
    actorAddress: "",
    ownerAddress: "",
    caller: "",
    requiredConfirm: 0,
    status: 0,
    currentConfirm: 0,
    confirmers: [],
    createdAt: undefined,
    validUntil: undefined,
    dataHashes: [],
    expiredHeight: 0,
    executionErrorMessage: "",
  };
}

export const SyncActionSigner: MessageFns<SyncActionSigner> = {
  encode(message: SyncActionSigner, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.chain !== "") {
      writer.uint32(18).string(message.chain);
    }
    if (message.actorAddress !== "") {
      writer.uint32(26).string(message.actorAddress);
    }
    if (message.ownerAddress !== "") {
      writer.uint32(34).string(message.ownerAddress);
    }
    if (message.caller !== "") {
      writer.uint32(42).string(message.caller);
    }
    if (message.requiredConfirm !== 0) {
      writer.uint32(48).uint64(message.requiredConfirm);
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.currentConfirm !== 0) {
      writer.uint32(64).uint64(message.currentConfirm);
    }
    for (const v of message.confirmers) {
      writer.uint32(74).string(v!);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    if (message.validUntil !== undefined) {
      Timestamp.encode(toTimestamp(message.validUntil), writer.uint32(90).fork()).join();
    }
    for (const v of message.dataHashes) {
      ContractInfoHash.encode(v!, writer.uint32(98).fork()).join();
    }
    if (message.expiredHeight !== 0) {
      writer.uint32(104).int64(message.expiredHeight);
    }
    if (message.executionErrorMessage !== "") {
      writer.uint32(114).string(message.executionErrorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncActionSigner {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncActionSigner();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.actorAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ownerAddress = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.caller = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.requiredConfirm = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.currentConfirm = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.confirmers.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.validUntil = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.dataHashes.push(ContractInfoHash.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.expiredHeight = longToNumber(reader.int64());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.executionErrorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncActionSigner {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      actorAddress: isSet(object.actorAddress) ? globalThis.String(object.actorAddress) : "",
      ownerAddress: isSet(object.ownerAddress) ? globalThis.String(object.ownerAddress) : "",
      caller: isSet(object.caller) ? globalThis.String(object.caller) : "",
      requiredConfirm: isSet(object.requiredConfirm) ? globalThis.Number(object.requiredConfirm) : 0,
      status: isSet(object.status) ? requestStatusFromJSON(object.status) : 0,
      currentConfirm: isSet(object.currentConfirm) ? globalThis.Number(object.currentConfirm) : 0,
      confirmers: globalThis.Array.isArray(object?.confirmers)
        ? object.confirmers.map((e: any) => globalThis.String(e))
        : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      validUntil: isSet(object.validUntil) ? fromJsonTimestamp(object.validUntil) : undefined,
      dataHashes: globalThis.Array.isArray(object?.dataHashes)
        ? object.dataHashes.map((e: any) => ContractInfoHash.fromJSON(e))
        : [],
      expiredHeight: isSet(object.expiredHeight) ? globalThis.Number(object.expiredHeight) : 0,
      executionErrorMessage: isSet(object.executionErrorMessage) ? globalThis.String(object.executionErrorMessage) : "",
    };
  },

  toJSON(message: SyncActionSigner): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.actorAddress !== "") {
      obj.actorAddress = message.actorAddress;
    }
    if (message.ownerAddress !== "") {
      obj.ownerAddress = message.ownerAddress;
    }
    if (message.caller !== "") {
      obj.caller = message.caller;
    }
    if (message.requiredConfirm !== 0) {
      obj.requiredConfirm = Math.round(message.requiredConfirm);
    }
    if (message.status !== 0) {
      obj.status = requestStatusToJSON(message.status);
    }
    if (message.currentConfirm !== 0) {
      obj.currentConfirm = Math.round(message.currentConfirm);
    }
    if (message.confirmers?.length) {
      obj.confirmers = message.confirmers;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.validUntil !== undefined) {
      obj.validUntil = message.validUntil.toISOString();
    }
    if (message.dataHashes?.length) {
      obj.dataHashes = message.dataHashes.map((e) => ContractInfoHash.toJSON(e));
    }
    if (message.expiredHeight !== 0) {
      obj.expiredHeight = Math.round(message.expiredHeight);
    }
    if (message.executionErrorMessage !== "") {
      obj.executionErrorMessage = message.executionErrorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncActionSigner>, I>>(base?: I): SyncActionSigner {
    return SyncActionSigner.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncActionSigner>, I>>(object: I): SyncActionSigner {
    const message = createBaseSyncActionSigner();
    message.id = object.id ?? 0;
    message.chain = object.chain ?? "";
    message.actorAddress = object.actorAddress ?? "";
    message.ownerAddress = object.ownerAddress ?? "";
    message.caller = object.caller ?? "";
    message.requiredConfirm = object.requiredConfirm ?? 0;
    message.status = object.status ?? 0;
    message.currentConfirm = object.currentConfirm ?? 0;
    message.confirmers = object.confirmers?.map((e) => e) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.validUntil = object.validUntil ?? undefined;
    message.dataHashes = object.dataHashes?.map((e) => ContractInfoHash.fromPartial(e)) || [];
    message.expiredHeight = object.expiredHeight ?? 0;
    message.executionErrorMessage = object.executionErrorMessage ?? "";
    return message;
  },
};

function createBaseParameterSyncSignerByOracle(): ParameterSyncSignerByOracle {
  return { chain: "", ownerAddress: "", actorAddress: "", expireEpoch: "" };
}

export const ParameterSyncSignerByOracle: MessageFns<ParameterSyncSignerByOracle> = {
  encode(message: ParameterSyncSignerByOracle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chain !== "") {
      writer.uint32(10).string(message.chain);
    }
    if (message.ownerAddress !== "") {
      writer.uint32(18).string(message.ownerAddress);
    }
    if (message.actorAddress !== "") {
      writer.uint32(26).string(message.actorAddress);
    }
    if (message.expireEpoch !== "") {
      writer.uint32(34).string(message.expireEpoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParameterSyncSignerByOracle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParameterSyncSignerByOracle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ownerAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.actorAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expireEpoch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParameterSyncSignerByOracle {
    return {
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      ownerAddress: isSet(object.ownerAddress) ? globalThis.String(object.ownerAddress) : "",
      actorAddress: isSet(object.actorAddress) ? globalThis.String(object.actorAddress) : "",
      expireEpoch: isSet(object.expireEpoch) ? globalThis.String(object.expireEpoch) : "",
    };
  },

  toJSON(message: ParameterSyncSignerByOracle): unknown {
    const obj: any = {};
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.ownerAddress !== "") {
      obj.ownerAddress = message.ownerAddress;
    }
    if (message.actorAddress !== "") {
      obj.actorAddress = message.actorAddress;
    }
    if (message.expireEpoch !== "") {
      obj.expireEpoch = message.expireEpoch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParameterSyncSignerByOracle>, I>>(base?: I): ParameterSyncSignerByOracle {
    return ParameterSyncSignerByOracle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParameterSyncSignerByOracle>, I>>(object: I): ParameterSyncSignerByOracle {
    const message = createBaseParameterSyncSignerByOracle();
    message.chain = object.chain ?? "";
    message.ownerAddress = object.ownerAddress ?? "";
    message.actorAddress = object.actorAddress ?? "";
    message.expireEpoch = object.expireEpoch ?? "";
    return message;
  },
};

function createBaseContractInfoHash(): ContractInfoHash {
  return { contractParam: undefined, hash: new Uint8Array(0), confirmers: [] };
}

export const ContractInfoHash: MessageFns<ContractInfoHash> = {
  encode(message: ContractInfoHash, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contractParam !== undefined) {
      ParameterSyncSignerByOracle.encode(message.contractParam, writer.uint32(10).fork()).join();
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    for (const v of message.confirmers) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContractInfoHash {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContractInfoHash();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.contractParam = ParameterSyncSignerByOracle.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirmers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContractInfoHash {
    return {
      contractParam: isSet(object.contractParam)
        ? ParameterSyncSignerByOracle.fromJSON(object.contractParam)
        : undefined,
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
      confirmers: globalThis.Array.isArray(object?.confirmers)
        ? object.confirmers.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ContractInfoHash): unknown {
    const obj: any = {};
    if (message.contractParam !== undefined) {
      obj.contractParam = ParameterSyncSignerByOracle.toJSON(message.contractParam);
    }
    if (message.hash.length !== 0) {
      obj.hash = base64FromBytes(message.hash);
    }
    if (message.confirmers?.length) {
      obj.confirmers = message.confirmers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContractInfoHash>, I>>(base?: I): ContractInfoHash {
    return ContractInfoHash.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContractInfoHash>, I>>(object: I): ContractInfoHash {
    const message = createBaseContractInfoHash();
    message.contractParam = (object.contractParam !== undefined && object.contractParam !== null)
      ? ParameterSyncSignerByOracle.fromPartial(object.contractParam)
      : undefined;
    message.hash = object.hash ?? new Uint8Array(0);
    message.confirmers = object.confirmers?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
