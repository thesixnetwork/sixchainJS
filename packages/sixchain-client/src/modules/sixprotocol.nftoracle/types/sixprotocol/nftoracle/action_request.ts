// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/nftoracle/action_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { DataHash, RequestStatus, requestStatusFromJSON, requestStatusToJSON } from "./request";

export const protobufPackage = "sixprotocol.nftoracle";

export interface ActionParameter {
  name: string;
  value: string;
}

export interface ActionOracleParam {
  nftSchemaCode: string;
  tokenId: string;
  action: string;
  params: ActionParameter[];
  refId: string;
  expiredAt: Date | undefined;
  onBehalfOf: string;
}

export interface ActionOracleRequest {
  id: number;
  nftSchemaCode: string;
  tokenId: string;
  action: string;
  params: ActionParameter[];
  caller: string;
  refId: string;
  requiredConfirm: number;
  status: RequestStatus;
  currentConfirm: number;
  confirmers: string[];
  createdAt: Date | undefined;
  validUntil: Date | undefined;
  dataHashes: DataHash[];
  expiredHeight: number;
  executionErrorMessage: string;
}

function createBaseActionParameter(): ActionParameter {
  return { name: "", value: "" };
}

export const ActionParameter: MessageFns<ActionParameter> = {
  encode(message: ActionParameter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionParameter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionParameter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionParameter {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ActionParameter): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionParameter>, I>>(base?: I): ActionParameter {
    return ActionParameter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionParameter>, I>>(object: I): ActionParameter {
    const message = createBaseActionParameter();
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseActionOracleParam(): ActionOracleParam {
  return { nftSchemaCode: "", tokenId: "", action: "", params: [], refId: "", expiredAt: undefined, onBehalfOf: "" };
}

export const ActionOracleParam: MessageFns<ActionOracleParam> = {
  encode(message: ActionOracleParam, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nftSchemaCode !== "") {
      writer.uint32(10).string(message.nftSchemaCode);
    }
    if (message.tokenId !== "") {
      writer.uint32(18).string(message.tokenId);
    }
    if (message.action !== "") {
      writer.uint32(26).string(message.action);
    }
    for (const v of message.params) {
      ActionParameter.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.refId !== "") {
      writer.uint32(42).string(message.refId);
    }
    if (message.expiredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiredAt), writer.uint32(50).fork()).join();
    }
    if (message.onBehalfOf !== "") {
      writer.uint32(58).string(message.onBehalfOf);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionOracleParam {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionOracleParam();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nftSchemaCode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tokenId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.params.push(ActionParameter.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.refId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.onBehalfOf = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionOracleParam {
    return {
      nftSchemaCode: isSet(object.nftSchemaCode) ? globalThis.String(object.nftSchemaCode) : "",
      tokenId: isSet(object.tokenId) ? globalThis.String(object.tokenId) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      params: globalThis.Array.isArray(object?.params)
        ? object.params.map((e: any) => ActionParameter.fromJSON(e))
        : [],
      refId: isSet(object.refId) ? globalThis.String(object.refId) : "",
      expiredAt: isSet(object.expiredAt) ? fromJsonTimestamp(object.expiredAt) : undefined,
      onBehalfOf: isSet(object.onBehalfOf) ? globalThis.String(object.onBehalfOf) : "",
    };
  },

  toJSON(message: ActionOracleParam): unknown {
    const obj: any = {};
    if (message.nftSchemaCode !== "") {
      obj.nftSchemaCode = message.nftSchemaCode;
    }
    if (message.tokenId !== "") {
      obj.tokenId = message.tokenId;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.params?.length) {
      obj.params = message.params.map((e) => ActionParameter.toJSON(e));
    }
    if (message.refId !== "") {
      obj.refId = message.refId;
    }
    if (message.expiredAt !== undefined) {
      obj.expiredAt = message.expiredAt.toISOString();
    }
    if (message.onBehalfOf !== "") {
      obj.onBehalfOf = message.onBehalfOf;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionOracleParam>, I>>(base?: I): ActionOracleParam {
    return ActionOracleParam.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionOracleParam>, I>>(object: I): ActionOracleParam {
    const message = createBaseActionOracleParam();
    message.nftSchemaCode = object.nftSchemaCode ?? "";
    message.tokenId = object.tokenId ?? "";
    message.action = object.action ?? "";
    message.params = object.params?.map((e) => ActionParameter.fromPartial(e)) || [];
    message.refId = object.refId ?? "";
    message.expiredAt = object.expiredAt ?? undefined;
    message.onBehalfOf = object.onBehalfOf ?? "";
    return message;
  },
};

function createBaseActionOracleRequest(): ActionOracleRequest {
  return {
    id: 0,
    nftSchemaCode: "",
    tokenId: "",
    action: "",
    params: [],
    caller: "",
    refId: "",
    requiredConfirm: 0,
    status: 0,
    currentConfirm: 0,
    confirmers: [],
    createdAt: undefined,
    validUntil: undefined,
    dataHashes: [],
    expiredHeight: 0,
    executionErrorMessage: "",
  };
}

export const ActionOracleRequest: MessageFns<ActionOracleRequest> = {
  encode(message: ActionOracleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.nftSchemaCode !== "") {
      writer.uint32(18).string(message.nftSchemaCode);
    }
    if (message.tokenId !== "") {
      writer.uint32(26).string(message.tokenId);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    for (const v of message.params) {
      ActionParameter.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.caller !== "") {
      writer.uint32(50).string(message.caller);
    }
    if (message.refId !== "") {
      writer.uint32(58).string(message.refId);
    }
    if (message.requiredConfirm !== 0) {
      writer.uint32(64).uint64(message.requiredConfirm);
    }
    if (message.status !== 0) {
      writer.uint32(72).int32(message.status);
    }
    if (message.currentConfirm !== 0) {
      writer.uint32(80).uint64(message.currentConfirm);
    }
    for (const v of message.confirmers) {
      writer.uint32(90).string(v!);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(98).fork()).join();
    }
    if (message.validUntil !== undefined) {
      Timestamp.encode(toTimestamp(message.validUntil), writer.uint32(106).fork()).join();
    }
    for (const v of message.dataHashes) {
      DataHash.encode(v!, writer.uint32(114).fork()).join();
    }
    if (message.expiredHeight !== 0) {
      writer.uint32(120).int64(message.expiredHeight);
    }
    if (message.executionErrorMessage !== "") {
      writer.uint32(130).string(message.executionErrorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionOracleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionOracleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nftSchemaCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tokenId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.params.push(ActionParameter.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.caller = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.refId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.requiredConfirm = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.currentConfirm = longToNumber(reader.uint64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.confirmers.push(reader.string());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.validUntil = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.dataHashes.push(DataHash.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.expiredHeight = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.executionErrorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionOracleRequest {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      nftSchemaCode: isSet(object.nftSchemaCode) ? globalThis.String(object.nftSchemaCode) : "",
      tokenId: isSet(object.tokenId) ? globalThis.String(object.tokenId) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      params: globalThis.Array.isArray(object?.params)
        ? object.params.map((e: any) => ActionParameter.fromJSON(e))
        : [],
      caller: isSet(object.caller) ? globalThis.String(object.caller) : "",
      refId: isSet(object.refId) ? globalThis.String(object.refId) : "",
      requiredConfirm: isSet(object.requiredConfirm) ? globalThis.Number(object.requiredConfirm) : 0,
      status: isSet(object.status) ? requestStatusFromJSON(object.status) : 0,
      currentConfirm: isSet(object.currentConfirm) ? globalThis.Number(object.currentConfirm) : 0,
      confirmers: globalThis.Array.isArray(object?.confirmers)
        ? object.confirmers.map((e: any) => globalThis.String(e))
        : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      validUntil: isSet(object.validUntil) ? fromJsonTimestamp(object.validUntil) : undefined,
      dataHashes: globalThis.Array.isArray(object?.dataHashes)
        ? object.dataHashes.map((e: any) => DataHash.fromJSON(e))
        : [],
      expiredHeight: isSet(object.expiredHeight) ? globalThis.Number(object.expiredHeight) : 0,
      executionErrorMessage: isSet(object.executionErrorMessage) ? globalThis.String(object.executionErrorMessage) : "",
    };
  },

  toJSON(message: ActionOracleRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.nftSchemaCode !== "") {
      obj.nftSchemaCode = message.nftSchemaCode;
    }
    if (message.tokenId !== "") {
      obj.tokenId = message.tokenId;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.params?.length) {
      obj.params = message.params.map((e) => ActionParameter.toJSON(e));
    }
    if (message.caller !== "") {
      obj.caller = message.caller;
    }
    if (message.refId !== "") {
      obj.refId = message.refId;
    }
    if (message.requiredConfirm !== 0) {
      obj.requiredConfirm = Math.round(message.requiredConfirm);
    }
    if (message.status !== 0) {
      obj.status = requestStatusToJSON(message.status);
    }
    if (message.currentConfirm !== 0) {
      obj.currentConfirm = Math.round(message.currentConfirm);
    }
    if (message.confirmers?.length) {
      obj.confirmers = message.confirmers;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.validUntil !== undefined) {
      obj.validUntil = message.validUntil.toISOString();
    }
    if (message.dataHashes?.length) {
      obj.dataHashes = message.dataHashes.map((e) => DataHash.toJSON(e));
    }
    if (message.expiredHeight !== 0) {
      obj.expiredHeight = Math.round(message.expiredHeight);
    }
    if (message.executionErrorMessage !== "") {
      obj.executionErrorMessage = message.executionErrorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionOracleRequest>, I>>(base?: I): ActionOracleRequest {
    return ActionOracleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionOracleRequest>, I>>(object: I): ActionOracleRequest {
    const message = createBaseActionOracleRequest();
    message.id = object.id ?? 0;
    message.nftSchemaCode = object.nftSchemaCode ?? "";
    message.tokenId = object.tokenId ?? "";
    message.action = object.action ?? "";
    message.params = object.params?.map((e) => ActionParameter.fromPartial(e)) || [];
    message.caller = object.caller ?? "";
    message.refId = object.refId ?? "";
    message.requiredConfirm = object.requiredConfirm ?? 0;
    message.status = object.status ?? 0;
    message.currentConfirm = object.currentConfirm ?? 0;
    message.confirmers = object.confirmers?.map((e) => e) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.validUntil = object.validUntil ?? undefined;
    message.dataHashes = object.dataHashes?.map((e) => DataHash.fromPartial(e)) || [];
    message.expiredHeight = object.expiredHeight ?? 0;
    message.executionErrorMessage = object.executionErrorMessage ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
