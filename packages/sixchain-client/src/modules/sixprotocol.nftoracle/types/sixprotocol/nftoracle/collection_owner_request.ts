// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: sixprotocol/nftoracle/collection_owner_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { RequestStatus, requestStatusFromJSON, requestStatusToJSON } from "./request";

export const protobufPackage = "sixprotocol.nftoracle";

export interface OriginContractParam {
  chain: string;
  contractAddress: string;
  contractOwner: string;
  requestExpire: Date | undefined;
}

export interface CollectionOwnerRequest {
  id: number;
  nftSchemaCode: string;
  signer: string;
  requiredConfirm: number;
  status: RequestStatus;
  currentConfirm: number;
  confirmers: string[];
  createdAt: Date | undefined;
  validUntil: Date | undefined;
  contractInfo: OriginContractInfo[];
  expiredHeight: number;
}

export interface OriginContractInfo {
  contractOriginDataInfo: OriginContractParam | undefined;
  hash: Uint8Array;
  confirmers: string[];
}

function createBaseOriginContractParam(): OriginContractParam {
  return { chain: "", contractAddress: "", contractOwner: "", requestExpire: undefined };
}

export const OriginContractParam: MessageFns<OriginContractParam> = {
  encode(message: OriginContractParam, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chain !== "") {
      writer.uint32(10).string(message.chain);
    }
    if (message.contractAddress !== "") {
      writer.uint32(18).string(message.contractAddress);
    }
    if (message.contractOwner !== "") {
      writer.uint32(26).string(message.contractOwner);
    }
    if (message.requestExpire !== undefined) {
      Timestamp.encode(toTimestamp(message.requestExpire), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OriginContractParam {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOriginContractParam();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contractAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contractOwner = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestExpire = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OriginContractParam {
    return {
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      contractAddress: isSet(object.contractAddress) ? globalThis.String(object.contractAddress) : "",
      contractOwner: isSet(object.contractOwner) ? globalThis.String(object.contractOwner) : "",
      requestExpire: isSet(object.requestExpire) ? fromJsonTimestamp(object.requestExpire) : undefined,
    };
  },

  toJSON(message: OriginContractParam): unknown {
    const obj: any = {};
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.contractAddress !== "") {
      obj.contractAddress = message.contractAddress;
    }
    if (message.contractOwner !== "") {
      obj.contractOwner = message.contractOwner;
    }
    if (message.requestExpire !== undefined) {
      obj.requestExpire = message.requestExpire.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OriginContractParam>, I>>(base?: I): OriginContractParam {
    return OriginContractParam.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OriginContractParam>, I>>(object: I): OriginContractParam {
    const message = createBaseOriginContractParam();
    message.chain = object.chain ?? "";
    message.contractAddress = object.contractAddress ?? "";
    message.contractOwner = object.contractOwner ?? "";
    message.requestExpire = object.requestExpire ?? undefined;
    return message;
  },
};

function createBaseCollectionOwnerRequest(): CollectionOwnerRequest {
  return {
    id: 0,
    nftSchemaCode: "",
    signer: "",
    requiredConfirm: 0,
    status: 0,
    currentConfirm: 0,
    confirmers: [],
    createdAt: undefined,
    validUntil: undefined,
    contractInfo: [],
    expiredHeight: 0,
  };
}

export const CollectionOwnerRequest: MessageFns<CollectionOwnerRequest> = {
  encode(message: CollectionOwnerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.nftSchemaCode !== "") {
      writer.uint32(18).string(message.nftSchemaCode);
    }
    if (message.signer !== "") {
      writer.uint32(26).string(message.signer);
    }
    if (message.requiredConfirm !== 0) {
      writer.uint32(32).uint64(message.requiredConfirm);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.currentConfirm !== 0) {
      writer.uint32(48).uint64(message.currentConfirm);
    }
    for (const v of message.confirmers) {
      writer.uint32(58).string(v!);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(66).fork()).join();
    }
    if (message.validUntil !== undefined) {
      Timestamp.encode(toTimestamp(message.validUntil), writer.uint32(74).fork()).join();
    }
    for (const v of message.contractInfo) {
      OriginContractInfo.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.expiredHeight !== 0) {
      writer.uint32(88).int64(message.expiredHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectionOwnerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectionOwnerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nftSchemaCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signer = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requiredConfirm = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.currentConfirm = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.confirmers.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.validUntil = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.contractInfo.push(OriginContractInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.expiredHeight = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectionOwnerRequest {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      nftSchemaCode: isSet(object.nftSchemaCode) ? globalThis.String(object.nftSchemaCode) : "",
      signer: isSet(object.signer) ? globalThis.String(object.signer) : "",
      requiredConfirm: isSet(object.requiredConfirm) ? globalThis.Number(object.requiredConfirm) : 0,
      status: isSet(object.status) ? requestStatusFromJSON(object.status) : 0,
      currentConfirm: isSet(object.currentConfirm) ? globalThis.Number(object.currentConfirm) : 0,
      confirmers: globalThis.Array.isArray(object?.confirmers)
        ? object.confirmers.map((e: any) => globalThis.String(e))
        : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      validUntil: isSet(object.validUntil) ? fromJsonTimestamp(object.validUntil) : undefined,
      contractInfo: globalThis.Array.isArray(object?.contractInfo)
        ? object.contractInfo.map((e: any) => OriginContractInfo.fromJSON(e))
        : [],
      expiredHeight: isSet(object.expiredHeight) ? globalThis.Number(object.expiredHeight) : 0,
    };
  },

  toJSON(message: CollectionOwnerRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.nftSchemaCode !== "") {
      obj.nftSchemaCode = message.nftSchemaCode;
    }
    if (message.signer !== "") {
      obj.signer = message.signer;
    }
    if (message.requiredConfirm !== 0) {
      obj.requiredConfirm = Math.round(message.requiredConfirm);
    }
    if (message.status !== 0) {
      obj.status = requestStatusToJSON(message.status);
    }
    if (message.currentConfirm !== 0) {
      obj.currentConfirm = Math.round(message.currentConfirm);
    }
    if (message.confirmers?.length) {
      obj.confirmers = message.confirmers;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.validUntil !== undefined) {
      obj.validUntil = message.validUntil.toISOString();
    }
    if (message.contractInfo?.length) {
      obj.contractInfo = message.contractInfo.map((e) => OriginContractInfo.toJSON(e));
    }
    if (message.expiredHeight !== 0) {
      obj.expiredHeight = Math.round(message.expiredHeight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CollectionOwnerRequest>, I>>(base?: I): CollectionOwnerRequest {
    return CollectionOwnerRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CollectionOwnerRequest>, I>>(object: I): CollectionOwnerRequest {
    const message = createBaseCollectionOwnerRequest();
    message.id = object.id ?? 0;
    message.nftSchemaCode = object.nftSchemaCode ?? "";
    message.signer = object.signer ?? "";
    message.requiredConfirm = object.requiredConfirm ?? 0;
    message.status = object.status ?? 0;
    message.currentConfirm = object.currentConfirm ?? 0;
    message.confirmers = object.confirmers?.map((e) => e) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.validUntil = object.validUntil ?? undefined;
    message.contractInfo = object.contractInfo?.map((e) => OriginContractInfo.fromPartial(e)) || [];
    message.expiredHeight = object.expiredHeight ?? 0;
    return message;
  },
};

function createBaseOriginContractInfo(): OriginContractInfo {
  return { contractOriginDataInfo: undefined, hash: new Uint8Array(0), confirmers: [] };
}

export const OriginContractInfo: MessageFns<OriginContractInfo> = {
  encode(message: OriginContractInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contractOriginDataInfo !== undefined) {
      OriginContractParam.encode(message.contractOriginDataInfo, writer.uint32(10).fork()).join();
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    for (const v of message.confirmers) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OriginContractInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOriginContractInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.contractOriginDataInfo = OriginContractParam.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirmers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OriginContractInfo {
    return {
      contractOriginDataInfo: isSet(object.contractOriginDataInfo)
        ? OriginContractParam.fromJSON(object.contractOriginDataInfo)
        : undefined,
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
      confirmers: globalThis.Array.isArray(object?.confirmers)
        ? object.confirmers.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: OriginContractInfo): unknown {
    const obj: any = {};
    if (message.contractOriginDataInfo !== undefined) {
      obj.contractOriginDataInfo = OriginContractParam.toJSON(message.contractOriginDataInfo);
    }
    if (message.hash.length !== 0) {
      obj.hash = base64FromBytes(message.hash);
    }
    if (message.confirmers?.length) {
      obj.confirmers = message.confirmers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OriginContractInfo>, I>>(base?: I): OriginContractInfo {
    return OriginContractInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OriginContractInfo>, I>>(object: I): OriginContractInfo {
    const message = createBaseOriginContractInfo();
    message.contractOriginDataInfo =
      (object.contractOriginDataInfo !== undefined && object.contractOriginDataInfo !== null)
        ? OriginContractParam.fromPartial(object.contractOriginDataInfo)
        : undefined;
    message.hash = object.hash ?? new Uint8Array(0);
    message.confirmers = object.confirmers?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
